export type AdminCategoryListItem = {
  id: string;
  name: string;
  slug: string;
  description: string;
  toolCount: number;
};

export type AdminCategoryDetail = {
  id: string;
  name: string;
  slug: string;
  description: string;
};
